const EN_LOCALE_ONLY = 'true'
const IN_BROWSER = typeof window !== 'undefined'
const IS_DEBUG = 'true'
const IS_PROD = 'production'

typeof exports === "object"
module.exports = {
  EN_LOCALE_ONLY,
  IN_BROWSER,
  IS_DEBUG,
  IS_PROD,
}
